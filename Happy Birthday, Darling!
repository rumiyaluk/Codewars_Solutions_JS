// https://www.codewars.com/kata/5e96332d18ac870032eb735f

function womensAge(n) {
  let a = n/2;
  if (n % 2 === 0) 
  return `${n}? That's just 20, in base ${Math.floor(a)}!` ;
  else return `${n}? That's just 21, in base ${Math.floor(a)}!`;
}
