// https://www.codewars.com/kata/draw-stairs

function drawStairs(n) {
  let a = '';
  for (let i = 0; i < n; i++) {
    if (i < n - 1) {
      a = a + ' '.repeat(i) + 'I\n';
      } else {a = a + ' '.repeat(i) + 'I';
    }
  } return a;
}
